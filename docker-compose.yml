services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: notifyhub
      POSTGRES_PASSWORD: notifyhub_dev_pw
      POSTGRES_DB: notifyhub_dev
    ports:
      - '5432:5432'
    volumes:
      - notifyhub_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U notifyhub -d notifyhub_dev']
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build:
      context: .
      target: runtime
    restart: unless-stopped
    ports:
      - '4000:4000'
    environment:
      NODE_ENV: production
      PORT: 4000
      DATABASE_URL: postgresql://notifyhub:notifyhub_dev_pw@postgres:5432/notifyhub_dev?schema=public
      JWT_SECRET: ${JWT_SECRET:?set a real JWT_SECRET in .env before running docker compose}
      JWT_EXPIRES_IN: 15m
      CORS_ORIGIN: '*'
    depends_on:
      postgres:
        condition: service_healthy
    command: sh -c "npx prisma migrate deploy && node dist/index.js"

volumes:
  notifyhub_postgres_data:
